import { types as helloWorldActions } from '../actions/hello-world-actions';

const initialState = {
    hello: []
};

const helloWorldReducer = (state = initialState, action) => {
    return state;
};

export default helloWorldReducer;
